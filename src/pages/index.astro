---
import BaseLayout from '../layouts/BaseLayout.astro';
import ProjectCard from '../components/ProjectCard.astro';
---

<BaseLayout title="Jan Petrlík - Systems Administrator & Infrastructure Engineer">
  <section class="hero">
    <h1>Jan Petrlík</h1>
    <p class="hero-subtitle">Systems Administrator & Infrastructure Engineer</p>
    <p class="hero-description">
      High school student in Czech Republic. I automate infrastructure with Ansible, 
      manage production systems, and occasionally reverse engineer things that shouldn't 
      need cloud services.
    </p>
    <div class="hero-links">
      <a href="/about" class="btn btn-primary">About Me</a>
      <a href="/projects" class="btn btn-secondary">Projects</a>
      <a href="https://github.com/jan64x" target="_blank" rel="noopener noreferrer" class="btn btn-secondary">
        <svg width="16" height="16" viewBox="0 0 16 16" fill="currentColor">
          <path d="M8 0C3.58 0 0 3.58 0 8c0 3.54 2.29 6.53 5.47 7.59.4.07.55-.17.55-.38 0-.19-.01-.82-.01-1.49-2.01.37-2.53-.49-2.69-.94-.09-.23-.48-.94-.82-1.13-.28-.15-.68-.52-.01-.53.63-.01 1.08.58 1.23.82.72 1.21 1.87.87 2.33.66.07-.52.28-.87.51-1.07-1.78-.2-3.64-.89-3.64-3.95 0-.87.31-1.59.82-2.15-.08-.2-.36-1.02.08-2.12 0 0 .67-.21 2.2.82.64-.18 1.32-.27 2-.27.68 0 1.36.09 2 .27 1.53-1.04 2.2-.82 2.2-.82.44 1.1.16 1.92.08 2.12.51.56.82 1.27.82 2.15 0 3.07-1.87 3.75-3.65 3.95.29.25.54.73.54 1.48 0 1.07-.01 1.93-.01 2.2 0 .21.15.46.55.38A8.013 8.013 0 0016 8c0-4.42-3.58-8-8-8z"></path>
        </svg>
        GitHub
      </a>
    </div>
  </section>

  <section class="section">
    <h2>What I Do</h2>
    <div class="highlights">
      <div class="highlight-card">
        <div class="highlight-icon">🖥️</div>
        <h3>Infrastructure Automation</h3>
        <p>Built a 10,500-line Ansible playbook that deploys my entire homelab. 16 VMs on Proxmox running everything from GitLab to self-hosted photo management.</p>
      </div>
      
      <div class="highlight-card">
        <div class="highlight-icon">🔌</div>
        <h3>Network Engineering</h3>
        <p>MikroTik routing, VLAN segmentation, and IPv6 deployment. Managing a /48 prefix because dynamic /64s are for people without plans.</p>
      </div>
      
      <div class="highlight-card">
        <div class="highlight-icon">🔓</div>
        <h3>Hardware Hacking</h3>
        <p>Reverse engineering IoT devices via UART. Added SSH and RTSP to Xiaomi cameras so they don't need to phone home to China.</p>
      </div>
    </div>
  </section>

  <section class="section">
    <h2>Featured Projects</h2>
    <div class="projects-grid">
      <ProjectCard
        title="Homelab Ansible Automation"
        description="Complete infrastructure-as-code for a 16-VM Proxmox homelab. TrueNAS, GitLab, monitoring stack, and everything in between."
        tags={['Ansible', 'Proxmox', 'Linux', 'Docker']}
        githubUrl="https://github.com/jan64x"
      />
      
      <ProjectCard
        title="Xiaomi Camera Hacking"
        description="Firmware modification via UART to add local SSH and RTSP. Because privacy matters and cloud dependencies don't."
        tags={['Reverse Engineering', 'Embedded', 'UART', 'IoT']}
      />
      
      <ProjectCard
        title="Solax Protocol Parser"
        description="Reverse engineered solar monitor WiFi protocol and built a Rust parser for local data extraction."
        tags={['Rust', 'Reverse Engineering', 'Hardware']}
      />
      
      <ProjectCard
        title="IPv6 Deployment"
        description="Full IPv6 via Hurricane Electric tunnel. Proper /48 allocation with per-VLAN /64 subnets."
        tags={['IPv6', 'Networking', 'MikroTik']}
      />
    </div>
    <div class="section-link">
      <a href="/projects" class="link-arrow">All projects →</a>
    </div>
  </section>

  <section class="section">
    <h2>Tech Stack</h2>
    <div class="tech-summary">
      <div class="tech-column">
        <h3>Infrastructure</h3>
        <ul>
          <li>Proxmox (Dell R620)</li>
          <li>Ansible automation</li>
          <li>Docker containers</li>
          <li>Prometheus/Grafana</li>
          <li>TrueNAS/ZFS</li>
        </ul>
      </div>
      
      <div class="tech-column">
        <h3>Networking</h3>
        <ul>
          <li>MikroTik RouterOS</li>
          <li>IPv6 deployment</li>
          <li>VLANs & firewall</li>
          <li>GNS3 simulation</li>
          <li>Nginx reverse proxy</li>
        </ul>
      </div>
      
      <div class="tech-column">
        <h3>Development</h3>
        <ul>
          <li>Python & Bash</li>
          <li>Rust (learning)</li>
          <li>C++ (embedded)</li>
          <li>Git/GitLab</li>
          <li>Web (JS/HTML/CSS)</li>
        </ul>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .hero {
    padding: var(--spacing-xl) 0;
    max-width: 800px;
  }
  
  .hero h1 {
    font-size: 3.5rem;
    margin-bottom: var(--spacing-sm);
    color: var(--text-primary);
  }
  
  .hero-subtitle {
    font-size: 1.5rem;
    color: var(--text-secondary);
    margin-bottom: var(--spacing-md);
  }
  
  .hero-description {
    font-size: 1.1rem;
    color: var(--text-secondary);
    line-height: 1.8;
    margin-bottom: var(--spacing-lg);
  }
  
  .hero-links {
    display: flex;
    gap: var(--spacing-md);
    flex-wrap: wrap;
  }
  
  .btn {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1.5rem;
    border-radius: 6px;
    font-weight: 500;
    transition: all 0.2s;
    border: 1px solid transparent;
  }
  
  .btn-primary {
    background-color: var(--accent);
    color: var(--bg-primary);
  }
  
  .btn-primary:hover {
    background-color: var(--accent-hover);
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(88, 166, 255, 0.3);
  }
  
  .btn-secondary {
    background-color: var(--bg-secondary);
    border-color: var(--border);
    color: var(--text-primary);
  }
  
  .btn-secondary:hover {
    border-color: var(--accent);
    background-color: var(--bg-tertiary);
    transform: translateY(-2px);
  }
  
  .section {
    margin-top: var(--spacing-xl);
    padding-top: var(--spacing-xl);
  }
  
  .highlights {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: var(--spacing-lg);
    margin-top: var(--spacing-lg);
  }
  
  .highlight-card {
    background-color: var(--bg-secondary);
    border: 1px solid var(--border);
    border-radius: 6px;
    padding: var(--spacing-lg);
    transition: border-color 0.2s, transform 0.2s;
  }
  
  .highlight-card:hover {
    border-color: var(--accent);
    transform: translateY(-4px);
  }
  
  .highlight-icon {
    font-size: 2rem;
    margin-bottom: var(--spacing-md);
  }
  
  .highlight-card h3 {
    margin-top: 0;
    margin-bottom: var(--spacing-sm);
    font-size: 1.2rem;
  }
  
  .highlight-card p {
    margin-bottom: 0;
    font-size: 0.95rem;
  }
  
  .projects-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    gap: var(--spacing-lg);
    margin-top: var(--spacing-lg);
  }
  
  .section-link {
    margin-top: var(--spacing-lg);
    text-align: center;
  }
  
  .link-arrow {
    font-size: 1.1rem;
    font-weight: 500;
    display: inline-flex;
    align-items: center;
  }
  
  .tech-summary {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: var(--spacing-lg);
    margin-top: var(--spacing-lg);
  }
  
  .tech-column {
    background-color: var(--bg-secondary);
    border: 1px solid var(--border);
    border-radius: 6px;
    padding: var(--spacing-lg);
  }
  
  .tech-column h3 {
    margin-top: 0;
    margin-bottom: var(--spacing-md);
    color: var(--accent);
    font-size: 1.1rem;
  }
  
  .tech-column ul {
    margin: 0;
    list-style: none;
  }
  
  .tech-column li {
    color: var(--text-secondary);
    padding: 0.4rem 0;
    padding-left: 1.5rem;
    position: relative;
  }
  
  .tech-column li::before {
    content: "▹";
    position: absolute;
    left: 0;
    color: var(--accent);
  }
  
  @media (max-width: 768px) {
    .hero h1 {
      font-size: 2.5rem;
    }
    
    .hero-subtitle {
      font-size: 1.2rem;
    }
    
    .hero-description {
      font-size: 1rem;
    }
    
    .highlights {
      grid-template-columns: 1fr;
    }
    
    .projects-grid {
      grid-template-columns: 1fr;
    }
  }
</style>