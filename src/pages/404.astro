---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout
  title="404 - Page Not Found | Jan Petrlík"
  description="The page you're looking for doesn't exist."
>
  <div class="error-container">
    <div class="error-code">404</div>
    <h1 class="error-title">Page Not Found</h1>
    <p class="error-description">
      This page doesn't exist, was moved, or was never deployed.
      Happens to the best of us.
    </p>
    <div class="terminal-block">
      <span class="prompt">$</span>
      <span class="command"> curl -I /wherever/you/were/going</span>
      <br />
      <span class="output">HTTP/2.0 404 Not Found</span>
    </div>
    <div class="error-actions">
      <a href="/" class="btn btn-primary">Go Home</a>
    </div>
  </div>
</BaseLayout>

<style>
  .error-container {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: var(--spacing-xl) 0;
    min-height: 60vh;
    gap: var(--spacing-md);
  }

  .error-code {
    font-size: 8rem;
    font-weight: 700;
    line-height: 1;
    color: var(--accent);
    font-family: var(--font-mono);
    user-select: none;
  }

  .error-title {
    font-size: 2rem;
    color: var(--text-primary);
    margin: 0;
  }

  .error-description {
    font-size: 1.1rem;
    color: var(--text-secondary);
    max-width: 480px;
    line-height: 1.7;
    margin: 0;
  }

  .terminal-block {
    background-color: var(--bg-secondary);
    border: 1px solid var(--border);
    border-radius: 6px;
    padding: 1rem 1.5rem;
    font-family: var(--font-mono);
    font-size: 0.875rem;
    text-align: left;
    width: 100%;
    max-width: 480px;
  }

  .prompt {
    color: var(--success);
    user-select: none;
  }

  .command {
    color: var(--text-primary);
  }

  .output {
    color: var(--warning);
  }

  .error-actions {
    display: flex;
    gap: var(--spacing-md);
    flex-wrap: wrap;
    justify-content: center;
    margin-top: var(--spacing-sm);
  }

  .btn {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1.5rem;
    border-radius: 6px;
    font-weight: 500;
    transition: all 0.2s;
    border: 1px solid transparent;
  }

  .btn-primary {
    background-color: var(--accent);
    color: var(--bg-primary);
  }

  .btn-primary:hover {
    background-color: var(--accent-hover);
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(88, 166, 255, 0.3);
  }

  .btn-secondary {
    background-color: var(--bg-secondary);
    border-color: var(--border);
    color: var(--text-primary);
  }

  .btn-secondary:hover {
    border-color: var(--accent);
    background-color: var(--bg-tertiary);
    transform: translateY(-2px);
  }
</style>
